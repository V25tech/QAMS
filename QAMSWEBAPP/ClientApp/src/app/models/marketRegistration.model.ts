export interface MarketRegistration {
    name:string
    uniqueCode:string
    remarks:string
  
};
export interface Markets {
    id: string
    name: string
    initiateBy: string
    createdDate: string | Date
};